#!/usr/bin/env bash

function get_array_count() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='get-array-count' --bold=' has been deprecated in favor of ' --code='echo-trim-empty-lines'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	get_array_count "$@"
fi
