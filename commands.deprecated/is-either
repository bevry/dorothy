#!/usr/bin/env bash

function is_either() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='is-either' --bold=' has been deprecated in favor of ' --code='is-needle'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	is_either "$@"
fi
