#!/usr/bin/env bash

function fs_absolute() (
	dorothy-warnings crash --code='fs-absolute' --bold=' has been deprecated in favor of ' --code='fs-path --absolute'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	fs_absolute "$@"
fi
