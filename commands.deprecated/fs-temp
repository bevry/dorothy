#!/usr/bin/env bash

function fs_temp() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='fs-temp' --bold=' has been renamed to ' --code='fs-make'
	fs-make "$@"
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	fs_temp "$@"
fi
