#!/usr/bin/env bash

function fs_rm() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='fs-rm' --bold=' has been renamed to ' --code='fs-remove'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	fs_rm "$@"
fi
