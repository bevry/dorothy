#!/usr/bin/env bash

function get_array() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='get-array' --bold=' has been deprecated in favor of ' --code='__array'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	get_array "$@"
fi
