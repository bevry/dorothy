#!/usr/bin/env bash

function get_git_default_branch() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='get-git-default-branch' --bold=' has been deprecated in favor of ' --code='git-helper default-branch'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	get_git_default_branch "$@"
fi
