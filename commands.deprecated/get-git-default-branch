#!/usr/bin/env bash

function get_git_default_branch() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings add --code='get-git-default-branch' --bold=' has been deprecated in favor of ' --code='git-helper default-branch'

	# =====================================
	# Arguments

	function help {
		__print_help "$@" <<-EOF || return $?
			ABOUT:
			Get the default branch of the local repository.

			USAGE:
			get-git-default-branch
		EOF
		return 22 # EINVAL 22 Invalid argument
	}

	# process
	local item
	while [[ $# -ne 0 ]]; do
		item="$1"
		shift
		case "$item" in
		'--help' | '-h') help ;;
		'--'*) help 'An unrecognised flag was provided: ' --variable-value={item} ;;
		*) help 'An unrecognised argument was provided: ' --variable-value={item} ;;
		esac
	done

	# =======================================================
	# Action

	git-helper default-branch
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	get_git_default_branch "$@"
fi
