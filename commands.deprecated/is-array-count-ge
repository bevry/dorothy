#!/usr/bin/env bash

function is_array_count_ge() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='is-array-count-ge' --bold=' has been deprecated in favor of ' --code='echo-trim-empty-lines'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	is_array_count_ge "$@"
fi
