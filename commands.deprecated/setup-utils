#!/usr/bin/env bash

function setup_utils() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='setup-utils' --bold=' has been deprecated in favor of ' --code='setup-util ...<utility>'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	setup_utils "$@"
fi
