#!/usr/bin/env bash

function is_array_empty() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='is-array-empty' --bold=' has been deprecated in favor of ' --code='is-whitespace'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	is_array_empty "$@"
fi
