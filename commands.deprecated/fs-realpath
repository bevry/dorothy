#!/usr/bin/env bash

function fs_realpath() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='fs-realpath' --bold=' has been deprecated in favor of ' --code='fs-path --physical'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	fs_realpath "$@"
fi
