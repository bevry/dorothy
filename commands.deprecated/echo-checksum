#!/usr/bin/env bash

function echo_checksum() (
	source "$DOROTHY/sources/stdinargs.bash"
	dorothy-warnings crash --code='echo-checksum' --bold=' has been deprecated in favor of ' --code='checksum'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	echo_checksum "$@"
fi
