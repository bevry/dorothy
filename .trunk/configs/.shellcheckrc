# https://github.com/trunk-io/configs
enable=all
source-path=SCRIPTDIR
# Disabling external sources is necessary otherwise shellcheck will take forever due to the size of `bash.bash`
external-sources=false
disable=SC1083,SC1090,SC1091,SC2016,SC2059,SC2119,SC2120,SC2154,SC2164,SC2249,SC2250,SC2310,SC2312,SC2317,SC2319,SC2329
# https://www.shellcheck.net/wiki/SC1083 False positives, we use `{...}` in the same way `exec` does, for variable assignment.
# https://www.shellcheck.net/wiki/SC1090 We disable external sources for performance.
# https://www.shellcheck.net/wiki/SC1091 We disable external sources for performance.
# https://www.shellcheck.net/wiki/SC2016 False positives, interferes with regular expressions.
# https://www.shellcheck.net/wiki/SC2059 False positives, interferes with intention.
# https://www.shellcheck.net/wiki/SC2119 False positives, interferes with library and help functions.
# https://www.shellcheck.net/wiki/SC2120 False positives, interferes with library and help functions.
# https://www.shellcheck.net/wiki/SC2154 False positives, cannot detect `eval` and other reference usage.
# https://www.shellcheck.net/wiki/SC2164 False positives, we use `errexit`.
# https://www.shellcheck.net/wiki/SC2249 Interferes with intention.
# https://www.shellcheck.net/wiki/SC2250 Leave it up to context.
# https://www.shellcheck.net/wiki/SC2310 We use `__...` safety functions, and have our own checks for when unsafe functions are invoked in a conditional.
# https://www.shellcheck.net/wiki/SC2312 False positives, we use `pipefail`.
# https://www.shellcheck.net/wiki/SC2317 False positives, seems broken.
# https://www.shellcheck.net/wiki/SC2319 False positives on `[[ $1 -eq $2 ]] || return $?`.
# https://www.shellcheck.net/wiki/SC2329 False positives, cannot detect `eval`, `try`, `do` invocations.
