#!/usr/bin/env dash

if [ "$0" = '-dash' ] || [ "$0" = 'dash' ]; then
	. "${DOROTHY:-"${XDG_DATA_HOME:-"$HOME/.local/share/dorothy"}"}/init.sh"
elif [ -n "${CI-}" ]; then
	echo "dash did not load Dorothy on CI because \$0 = $0" >&2
fi
