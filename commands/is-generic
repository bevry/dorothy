#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

function is-generic() (
	local username
	username="${1:-"$(get-profile username)"}"
	[[ $username =~ ubuntu|root|admin|super|user ]]
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	is-generic "$@"
fi
