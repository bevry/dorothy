#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

function setup-util-krypton() (
	setup-util "$@" --name='Krypton' --cli='krgpg' \
		BINARY='https://krypt.co/kr'
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	setup-util-krypton "$@"
fi
