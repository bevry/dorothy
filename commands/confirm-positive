#!/usr/bin/env bash
source "$BDIR/sources/strict.bash"

read -t 15 -r -n 1 -p "${1?"USAGE: confirm-positive <message>"} (Y/n) " answer
echo ""

# escape key should equal no
if test "$answer" = $'\x1b'; then
	exit 1
fi

# unless "n" was explicity specified, then return true
test "$answer" != "n"