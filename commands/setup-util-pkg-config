#!/usr/bin/env bash

# https://packages.debian.org/sid/amd64/pkg-config/filelist (no bins...)
# https://repology.org/project/pkg-config/versions

function setup_util_pkg_config() (
	source "$DOROTHY/sources/bash.bash"
	util_options=(
		--cli='pkg-config'
		# no apk, no mageia, gentoo
		APT='pkg-config'
		AUR='pkg-config'
		BREW='pkg-config'
		RPM='pkg-config'
		XBPS='pkg-config'
		ZYPPER='pkg-config'
	)
	setup-util "${util_options[@]}" "${setup_options[@]}"

)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	setup_util_pkg_config "$@"
fi
