#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

# trunk-ignore(shellcheck/SC2234)
function is-gnome() (
	test "${DESKTOP_SESSION-}" = 'gnome'
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	is-gnome "$@"
fi
