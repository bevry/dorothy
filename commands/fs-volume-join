#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

function fs-volume-join() (
	if [[ $2 == '/'* ]]; then
		fs-join "$1" "$2"
	else
		fs-join "$1" "$HOME/$2"
	fi
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	fs-volume-join "$@"
fi
