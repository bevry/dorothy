#!/usr/bin/env bash

options=$1
args="${@:2}"

# while [ $# -gt 0 ]; do
# 	if [ "$1" = '--' ]; then
# 		shift
# 		break
# 	fi
# 	options+=("$1")
# 	shift
# done

for i in "${@:2}"
do
	if [ "$i" = '--' ]; then
	    break
	fi
	options+=("$i")
done

echo ${options[@]}
