#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"
source "$DOROTHY/sources/splitlines.bash"

directories="$(expand-path "$(fs-join "$1" '*')")"

for directory in $directories; do
	echo "$directory"
done