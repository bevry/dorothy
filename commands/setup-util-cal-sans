#!/usr/bin/env bash

# https://github.com/calcom/font

function setup_util_cal_sans() (
	source "$DOROTHY/sources/util.bash"
	util_options=(
		--name='Cal Sans'
		--font='CalSans*'
		CASK='font-cal-sans'
	)

	# improve performance for detectable utilities when fetching asset configuration
	if setup-util check "${util_options[@]}" "${user_options[@]}"; then
		return 0
	fi

	# fetch and use configuration
	__fetch_and_add_download_option_via_slug_pathname 'calcom/font' 'fonts/otf/CalSans-Regular.otf' || return $?

	# setup
	setup-util "${util_options[@]}" "${user_options[@]}"
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	setup_util_cal_sans "$@"
fi
