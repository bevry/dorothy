#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

# temporary workaround, todo remove the need for this later
setup-util-realpath

if is-mac; then
	setup-mac-install
elif is-linux; then
	setup-linux-install
fi
