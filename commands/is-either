#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

function is-either() (
	local arg
	for arg in "${@:2}"; do
		if test "$1" = "$arg"; then
			return 0
		fi
	done
	return 1
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	is-either "$@"
fi
