#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

function is-dir() (
	local dir="$1"
	test -d "$dir"
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	is-dir "$@"
fi
