#!/usr/bin/env bash

# https://gitlab.gnome.org/chergert/ptyxis

# https://flathub.org/en/apps/app.devsuite.Ptyxis

# https://repology.org/project/ptyxis/versions

function setup_util_ptyxis() (
	source "$DOROTHY/sources/bash.bash"

	local options=(
		--app='Ptyxis'
		--app='org.gnome.Ptyxis' # .desktop
		--cli=ptyxis
		"$@"
		APK='ptyxis'
		AUR='ptyxis'
		DEB='ptyxis'
		FLATPAK='app.devsuite.Ptyxis'
		RPM='ptyxis'
		SOLUS='ptyxis'
		VOID='ptyxis'
		ZYPPER='ptyxis'
	)
	setup-util "${options[@]}"
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	setup_util_ptyxis "$@"
fi
