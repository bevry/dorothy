#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

function setup-util-curl() (
	setup-util "$@" NAME='curl' CLI='curl' \
		APK='curl' \
		APT='curl' \
		BREW='curl' \
		PACMAN='curl' \
		PAMAC='curl'
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	setup-util-curl "$@"
fi
