#!/usr/bin/env bash
source "$BDIR/sources/strict.bash"

# Switch to 1.1.1.1 to avoid filtering issues
select-dns cf

# Perform the upgdate
if is-mac; then
	setup-mac-update
elif is-linux; then
	setup-linux-update
fi

# Restore default DNS servers
select-dns
