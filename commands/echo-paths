#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"
source "$DOROTHY/sources/splitlines.bash"

if test -n "${1-}"; then
	pushd "$1" > /dev/null
fi
nodes="$(ls -1)"

for node in $nodes; do
	echo "$(realpath "$node")"
done

