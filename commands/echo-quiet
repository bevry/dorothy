#!/usr/bin/env bash

missing="$(get-flag-value missing -- "$@")"

get-flag-value verbose --missing="${VERBOSE-}" -- "$@" | echo-non-affirmative ||
	get-flag-value quiet --missing="${QUIET:-"$missing"}" -- "$@" | echo-affirmative || :
