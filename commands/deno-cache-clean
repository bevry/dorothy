#!/usr/bin/env bash

# help
if is-help "$@"; then
	cat <<-EOF >/dev/stderr
		ABOUT:
		??????
	EOF
	return 22 # Invalid argument
fi

if test -z "${DENO_DIR-}"; then
	DENO_DIR="$HOME/Library/Caches/deno"
fi

rm -Rfv "$DENO_DIR/deps" "$DENO_DIR/gen"
