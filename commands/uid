#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

# help
if is-help "$@"; then
	cat <<-EOF >/dev/stderr
		ABOUT:
		Get the user identifier for a username.

		USAGE:
		uid [username]
	EOF
	exit 22 # Invalid argument
fi

# options
username="${1:-"$(whoami)"}"

# act
id -u "$username"
