#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

# @todo figure out instlalers for other platforms

function setup-util-screen() (
	setup-util "$@" CLI='screen' \
		APT='screen' \
		BREW='screen' \
		YUM='screen'
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	setup-util-screen "$@"
fi
