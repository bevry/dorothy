#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

function setup-util-realpath() (
	setup-util "$@" --cli=realpath \
		BREW='coreutils'
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	setup-util-realpath "$@"
fi
