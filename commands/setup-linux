#!/usr/bin/env bash

function setup_linux() (
	source "$DOROTHY/sources/bash.bash"
	dorothy-warnings crash --code='setup-linux' --bold=' has been deprecated in favor of ' --code='setup-system'
)

# fire if invoked standalone
if [[ $0 == "${BASH_SOURCE[0]}" ]]; then
	setup_linux "$@"
fi
