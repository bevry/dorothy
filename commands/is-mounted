#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

mount_source="$(ask --question="Enter the mount source path." --default="${1-}" --required)"
mount_target="$(ask --question="Enter the mount target path." --default="${2-}" --required)"

mount | grep "${mount_source} on ${mount_target} type "
