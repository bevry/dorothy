#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

# timeout one hour
ec=0; read -t 3600 -r -n 1 -p "${1-}" || ec="$?"
if test "$ec" -gt 128; then
	exit 62 # Timer expired
elif test "$ec" -ne 0; then
	exit "$ec"
fi
