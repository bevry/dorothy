#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

append=''
if [[ $1 == '--append='* ]]; then
	append="${1:9}"
	shift
fi

# https://stackoverflow.com/a/1603436/130638
sed -e '/^$/,$d' "$@"

if test -n "$append"; then
	echo -n "$append"
fi
