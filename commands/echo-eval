#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

cmd="$*"
function leave () {
	local ec="$?"
	echo -e "\e[01;38m</$cmd>[$ec]\e[0m"
	exit "$ec"
}
trap leave EXIT

echo -e "\e[01;38m<$cmd>\e[0m"

# eval "$@" will mangle spaces and other things that need escaping
( "$@" )
