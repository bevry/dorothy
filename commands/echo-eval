#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

args="$@"
function leave () {
	local ec="$?"
	echo -e "\e[01;38m"
	echo "</${args[*]}>[$ec]"
	echo -e "\e[0m"
	exit "$ec"
}
trap leave EXIT

echo -e "\e[01;38m"
echo "<${args[*]}>"
echo -e "\e[0m"

eval "${args[@]}"
