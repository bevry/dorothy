#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

# [fd-find](https://github.com/sharkdp/fd)
# find alternative

# APT=fd-find \ - cargos currently have a bug on ubuntu

env CLI='fd' \
	APK='fd' \
	BREW='fd' \
	CARGO='fd-find' \
	CHOCO='fd' \
	DNF='fd-find' \
	EMERGE='fd' \
	NIX='fd' \
	NPM='fd-find' \
	PACMAN='fd' \
	PKG='fd-find' \
	PORT='fd' \
	SCOOP='fd' \
	XBPS='fd' \
	ZYPPER='fd' \
	setup-util
