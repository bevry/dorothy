#!/usr/bin/env bash

# fish also supports: "$@" ^ /dev/null

# help
if is-help "$@"; then
	cat <<-EOF >/dev/stderr
		ABOUT:
		    Send output to dev null.
	EOF
	exit 22 # Invalid argument
fi

("$@") 2>/dev/null
