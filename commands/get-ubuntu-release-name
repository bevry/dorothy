#!/usr/bin/env bash
source "$DOROTHY/sources/strict.bash"

function get-ubuntu-release-name() (
	lsb_release --codename --short
)

# fire if invoked standalone
if test "$0" = "${BASH_SOURCE[0]}"; then
	get-ubuntu-release-name "$@"
fi
