#!/bin/bash

# Android Simulator
function androiddev {
	local app; app=$(getapp "Android Studio.app")
	if is_dir "app"; then
		"$app/sdk/tools/emulator" -avd basic
	else
		echo "Android Studio is not installed"
	fi
}

androiddev