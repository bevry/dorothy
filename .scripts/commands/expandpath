#!/usr/bin/env bash
shopt -s nullglob globstar

function echolines {
	for arg in "$@"; do
		echo "$arg"
	done
}

# converts the intuitive
# expandpath '/Applications/Visual *'
# into the correct
# expandpath '/Applications/Visual\\ *'
for arg in "$@"; do
	eval echolines "${arg/ /\\ }"
done
