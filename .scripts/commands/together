#!/usr/bin/env bash

export command="tmux new session \; set-option -g default-shell '$(which bash)'"

for arg in "$@"
do
   command="$command \; split-window \; send-keys '$arg && exit' C-m"
done

eval "$command"