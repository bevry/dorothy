#!/usr/bin/env bash

if [[ "$BASH_VERSION" = "4."* ]]; then
	if test "$1" = "-v" -o "$1" = "--verbose"; then
		echo 'yes'
	fi
	exit 0
else
	if test "$1" = "-v" -o "$1" = "--verbose"; then
		echo 'no'
	fi
	exit 1
fi