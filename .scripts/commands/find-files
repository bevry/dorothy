#!/usr/bin/env bash
set -e
if is_latest_bash; then
	shopt -s globstar
fi

if is_string "$2"; then
	find . -path "**/*.$1" -exec "$2" {} \;
elif is_string "$1"; then
	find . -path "**/*.$1"
else
	echo "findfiles <extension> [command]"
fi