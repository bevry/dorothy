#!/usr/bin/env bash
set -e

if command_exists pip3; then
	pip3 "$@"
elif command_exists pip2; then
	pip2 "$@"
elif command_exits /usr/local/bin/pip; then
	/usr/local/bin/pip "$@"
else
	echo "could not find pip3 or pip2 or pip"
	exit 1
fi
