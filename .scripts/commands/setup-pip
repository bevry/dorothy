#!/usr/bin/env bash

set -e

if ! command_exists pip; then
	echo -e "\nPython (pip) does not seem to be installed..."
	exit
fi

echo -e "\nUpdating and installing python packages..."
pip install --upgrade pip
pip install httpie
