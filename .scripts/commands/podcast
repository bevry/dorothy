#!/usr/bin/env bash
set -e

if is_empty_string "$1"; then
	echo "podcast <in>"
	exit 1
else
	out="${1%.*}-aac_he.m4a"
	ffmpeg -i "$1" -c:a libfdk_aac -profile:a aac_he -b:a 48k "$out"
	#avconv -i "$1" -vn -c:a libfdk_aac -profile:a aac_he -b:a 48k "$out"
fi
