#!/usr/bin/env bash
set -e

export p="$1"
if is-empty-string "$p"; then
	p="."
fi

# Remove/reset all permissions on the path
sudo chmod -R -N "$p"

# Set the owner of the path to the current user
sudo chown -R "$USER" "$p"
