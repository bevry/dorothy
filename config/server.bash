#!/usr/bin/env bash
# do not use `export` keyword in this file:
# shellcheck disable=SC2034

# Used by `setup-server`

# The utilities (if any) to be installed and configured for this server:
# local SHARE_UTILS=(
# 	# 'gocryptfs'
# 	# 'nordvpn'
# 	# 'plex'
# 	# 'samba'
# 	# 'syncthing'
# 	# 'transmission'
# )

# The username, group, and password for the user that will be created and associated with the services and mounts
# local SHARE_USER='' SHARE_GROUP='' SHARE_PASSWORD=''

# Optional [mount-helper] arguments for mounting the drive, vault, and samba shares (if any)
# local DRIVE_MOUNT=() VAULT_MOUNT=() SAMBA_MOUNT=()

# Optional customisation of where these services store their data:
# local PLEX_HOME='' SYNCTHING_HOME='' TRANSMISSION_HOME=''

# Optional NordVPN configuration:
# local NORDVPN_TOKEN='' NORDVPN_DNS='' # 1.1.1.1 or 192.168.5.20 or whatever
