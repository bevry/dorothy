#!/usr/bin/env nu
# Used by `interactive.nu`
# Do whatever you want in this file

# Disable welcome banner
# $env.config.show_banner = false

# Inherited into `theme.nu` to load the desired theme, use `dorothy theme` to (re)configure this
# $env.DOROTHY_THEME = ''
