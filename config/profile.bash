#!/usr/bin/env bash
# shellcheck disable=SC2034
# Used by `get-profile`, which is used by `setup-git`, `setup-node`, use `setup-git --configure` to (re)configure this
# Do not use `export` keyword in this file

# NAME=''
# EMAIL=''
# URL=''
# USERNAME=''
# POSSESSIVE_PRONOUN='their'
