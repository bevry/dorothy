#!/usr/bin/env bash
# do not use `export` keyword in this file:
# shellcheck disable=SC2034

# Used by `ssh-helper configure` which is called by `setup-git`

# SSH_IDENTITY_AGENT='' # use '1password' for Krypt.co
